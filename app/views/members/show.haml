.container
  .page-header
    %h1
      =t(:'members.caption.name', title: @member.title, first_name: @member.first_name, last_name: @member.last_name)
      %small= t(:'clubs.club_title', name: @club.name)

  = render 'attributes', editable_class: 'edit-member', table_head_attributes: {id: 'member', :'data-leo-id' => @member.oid, :'data-member-update-url' => member_path(id: @member.oid)}

:javascript
  MyApp.initFunction(MyApp.initEditMember);